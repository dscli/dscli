{
  "name": "Go测试规范",
  "description": "Go语言测试最佳实践",
  "content": {
    "trigger": ["test", "测试", "go test", "单元测试"],
    "rules": [
      "测试文件应以_test.go结尾",
      "测试函数名应以Test开头",
      "使用表格驱动测试（Table-Driven Tests）",
      "每个测试用例应有清晰的描述",
      "测试应覆盖边界情况和错误情况"
    ],
    "examples": [
      "func TestAdd(t *testing.T) {\n    tests := []struct {\n        name string\n        a, b int\n        want int\n    }{\n        {\"正数相加\", 1, 2, 3},\n        {\"负数相加\", -1, -2, -3},\n        {\"零值相加\", 0, 0, 0},\n    }\n    for _, tt := range tests {\n        t.Run(tt.name, func(t *testing.T) {\n            got := Add(tt.a, tt.b)\n            if got != tt.want {\n                t.Errorf(\"Add(%d, %d) = %d, want %d\", tt.a, tt.b, got, tt.want)\n            }\n        })\n    }\n}"
    ]
  },
  "category": "go",
  "priority": 90,
  "is_global": true
}