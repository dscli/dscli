# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
dscli
dscli.org
# Build directory
build/

# Org-mode files (migrated to Markdown)

# Org-mode files in root directory (migrated to Markdown)

# Org-mode files in root directory (migrated to Markdown)
*.org
