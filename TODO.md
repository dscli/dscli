# 📋 dscli项目TODO事项

## 🎯 当前工作重点
**系统提示词重构** - 使用Go Template优化系统提示词，将变量作为参数传递

## 📊 项目状态概览

### 一、已完成的任务（✅）
1. **客户端网络重试机制** - 任务1.1.1 ✅
2. **write_file扩展增强** - 任务1.6 ✅
3. **read_file扩展增强** - 任务1.7 ✅
4. **search_file_with_pattern工具** - 任务1.8 ✅
5. **issue update命令** - 任务1.15.1 ✅
6. **issue close/reopen快捷命令** - 任务1.15.2 ✅
7. **issue系统重构为公共函数库** - 任务1.15.5 ✅
8. **execute_script工具重命名拆分** - 任务1.14 ✅

### 二、进行中的任务（🔄）
1. **issue assign快捷命令** - 任务1.15.3 🔄（基本完成，需要进一步测试）
   - 当前问题：API返回405错误（issue #11）

### 三、新完成的代码结构感知工具（✅ 2026-03-08）
✅ **已完成四个代码操作工具的开发：**

1. **`read_code_structure`** - 读取代码文件的结构信息（函数、类、方法、导入等）
2. **`read_code_section`** - 基于代码结构定位并读取特定代码片段
3. **`write_code_section`** - 基于代码结构定位并修改特定代码片段
4. **`search_code_semantic`** - 基于语义搜索代码中的特定模式

**主要功能特点：**
- 支持函数、类、方法、导入等代码结构的识别
- 支持基于代码结构的精确定位
- 支持语义搜索和上下文显示
- 支持dry-run模式预览修改
- 工具注册名避免冲突（如 `search_code_semantic` 避免与 `file.go` 冲突）

**技术要点：**
- 使用Go标准库的 `go/parser` 和 `go/ast` 进行代码解析
- 实现智能的上下文管理和重叠区域避免
- 支持可选参数和默认值
- 完整的错误处理和参数验证

所有工具已成功注册到系统中，可以通过chat命令调用。

### 四、高优先级待办任务（🔥）
1. **issue assign快捷命令** - 任务1.15.3 🔄（基本完成，需要进一步测试）
   - 当前问题：API返回405错误（issue #11）

#### A. 测试覆盖完善（最高优先级）
#### A. 测试覆盖完善（最高优先级）
- **任务0.1**：为缺失测试的源文件添加测试
  - `balance.go` → 需要 `balance_test.go`
  - `client.go` → 需要 `client_test.go`
  - `fim.go` → 需要 `fim_test.go`
  - `models.go` → 需要 `models_test.go`
  - `prompt.go` → 需要 `prompt_test.go`
  - `skills.go` → 需要实现功能（已有 `skills_test.go`）
  - `types.go` → 需要 `types_test.go`

#### B. 文件工具改进（基于实际使用痛点）
1. **任务1.14：file_search_replace工具** 🔥
   - 描述：解决大文件编辑中的重复内容问题
   - 优先级：高（已在issue #10中创建）
   - 功能：搜索替换、预览、统计等

2. **任务1.15：file_insert_with_pattern工具** 🔥
   - 描述：基于模式匹配插入内容，避免精确计算行号
   - 优先级：高
   - 功能：支持before/after/replace插入位置

#### C. 工具增强与可靠性
1. **任务1.1：工具调用重试机制** 🔥
   - 描述：为所有工具调用添加指数退避重试机制
   - 文件：`tools.go`
   - 功能：智能重试策略

2. **任务1.2：工具调用超时控制** 🔥
   - 描述：为工具调用添加超时控制
   - 相关函数：`ToolDef` 结构体添加 `Timeout` 字段

#### D. Issue管理完善
1. **任务1.15.4：增强issue list过滤功能** 🔥
   - 描述：添加按标签、作者、负责人、时间范围过滤
   - 文件：`issue.go`, `issue_test.go`

### 四、中优先级待办任务（⚡）

#### A. 文件工具改进
1. **任务1.9：增强search_files输出格式** ⚡
2. **任务1.10：为search_file_with_pattern添加正则表达式支持** ⚡
3. **任务1.11：添加缺失的文件系统工具** ⚡
   - `file_info`、`list_directory`、`create_directory`等

#### B. 高级功能增强
1. **任务1.16：系统提示词可定制化** ⚡
2. **任务1.17：skills系统完善** ⚡
3. **任务1.18：配置reasoner为chat的工具** ⚡

### 五、当前需要立即处理的问题

#### 1. 紧急问题
- **issue #11**：`issue_assign` 工具API调用失败：405 METHOD_NOT_ALLOWED
  - 需要调查GitCode API的assign接口问题

#### 2. 测试文件中的TODO
- 多个测试文件中有"TODO: Add test cases."注释
- 需要完善测试用例

#### 3. 当前Git状态
- 有3个文件变更未提交：
  - M client_test.go
  - M file_test.go
  - ?? test_file_tools.go（新文件）

### 六、建议的下一步行动

#### 立即行动（今天）：
1. **解决issue #11**：调查issue_assign的405错误
2. **开始实现file_search_replace工具**（issue #10）
3. **提交当前的Git变更**

#### 短期行动（本周）：
1. **完成缺失的测试文件**（任务0.1）
2. **实现file_insert_with_pattern工具**（任务1.15）
3. **完善issue list过滤功能**（任务1.15.4）

#### 中期行动：
1. **工具重试机制**（任务1.1）
2. **工具超时控制**（任务1.2）
3. **skills系统完善**（任务1.17）

### 七、架构约束提醒
1. **所有Go代码文件必须在项目根目录下**，不创建新的子目录
2. **每个`.go`文件必须有对应的`.go_test`文件**
3. **日期处理必须使用动态日期**，不能硬编码

---

## 🚀 下一步工作建议

### 1. 系统提示词重构（当前工作）
**目标**：使用Go Template重构系统提示词，将变量作为参数传递

**实施步骤**：
1. 分析当前系统提示词的结构和变量
2. 创建模板数据结构
3. 实现模板渲染函数
4. 更新GetSystemPrompt函数使用模板
5. 添加测试用例

**预期收益**：
- 提高代码可维护性
- 便于提示词定制化
- 支持动态变量注入

### 2. 文件工具改进（高优先级）
**目标**：实现file_search_replace工具，解决大文件编辑痛点

**实施步骤**：
1. 设计工具API接口
2. 实现搜索替换核心逻辑
3. 添加预览和确认机制
4. 实现统计功能
5. 添加测试用例

### 3. 测试覆盖完善（基础质量）
**目标**：为所有源文件添加测试覆盖

**实施策略**：
1. 按优先级顺序逐个文件添加测试
2. 先覆盖核心功能，再覆盖边缘情况
3. 确保测试质量，不仅仅是数量

---

## 📝 注意事项

1. **代码质量**：保持代码简洁，遵循最佳实践，添加必要注释
2. **错误处理**：考虑边界情况和错误处理
3. **向后兼容**：确保重构不影响现有功能
4. **文档更新**：及时更新相关文档
5. **Git提交**：及时提交重要更改，保持提交信息清晰

---

## 🔄 更新记录
- **2026-03-07**：创建TODO.md，记录当前项目状态和下一步建议
- **当前重点**：系统提示词重构（使用Go Template）

---

**保持专注，逐步推进，质量优先！**