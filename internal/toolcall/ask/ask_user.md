Ask user for clarification or feedback.

Ask the user when requirements are unclear, resources are
insufficient, or to request expert review of the current plan.

Timeout: default 3600s. Set `timeout` (seconds) to override — set shorter (e.g. 300) for simple confirmations.