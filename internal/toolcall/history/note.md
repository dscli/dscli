Summarize session for future recall

Record a key summary of the current conversation. Call at the
end of a conversation.

Content: 40 characters or less, containing key events and
keywords (e.g., "Implemented recall tool with session_id
filter"). Auto-truncates content over 40 characters.
