# ainap

Put the current AI session to sleep for a specified duration.

- `seconds` (required): number of seconds to sleep
- `reason` (optional): reason for sleeping, included in the return message

```
ainap(seconds=300, reason="Waiting for user feedback")
```
