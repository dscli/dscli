# aistatus

List all projects with their AI maintainer and status.

No parameters required. Returns a table showing for each project: ID, path, maintainer, status (`on`/`nap`/`off`), and creation time.

```
aistatus()
```
