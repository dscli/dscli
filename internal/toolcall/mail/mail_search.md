# mail_search

Search mails using FTS5 full-text search.

Full-text search across all mail subject and body content.

Parameters: query (required) — search query; limit (optional
integer, default 10, max 50).
