# listmail

List recent mail messages for the current maintainer.

List mail in the current maintainer's inbox, showing subject
only (no body). Optionally filter to unread only.

Parameters: unread_only (optional boolean, default false) —
show only unread mails; limit (optional integer, default 20,
max 100).
